# Pacotes @zarogmer vêm do GitHub Packages (privado). Requer GITHUB_TOKEN
# com escopo read:packages no ambiente (local: .env; deploy: env do Railway).
@zarogmer:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
